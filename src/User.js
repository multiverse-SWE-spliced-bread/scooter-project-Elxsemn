class User {
    // User code here
      #password
      constructor(username, password, email) {
          this.username = username
          this.age = this.age
          this.#password = password
          this.email = email
      }
  
  
      rentScooter (scooterID) {
          book.isAvailable = false
          book.currentOwner = this.username
          this.scooterID.push(scooters)
      }
  
      returnScooter (scooterID) {
          scooterID.isAvailable = true
          scooterID.currentOwner = null
      }
  
  }

  
  module.exports = User